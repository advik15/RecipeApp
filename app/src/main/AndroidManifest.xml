<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.final_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_Project">
        <activity android:name=".recipesAdapter2"
            tools:ignore="Instantiatable" />
        <activity android:name=".FavoriteRecipes" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".account_info" />
        <activity android:name=".User_Info" />
        <activity android:name=".register_user" />
        <activity android:name=".RecipeMaker" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Register_Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>